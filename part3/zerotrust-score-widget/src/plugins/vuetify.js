// src/plugins/vuetify.js
import { createVuetify } from 'vuetify';
import 'vuetify/styles'; // Ensure Vuetify's global styles are included

export default createVuetify({
  // Add other options here if needed
});
